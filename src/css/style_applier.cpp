#include "css/style_applier.h"

void apply_styles_to_tree(std::shared_ptr<Node> root)
{
    // Placeholder: no-op for now. Implementation will walk the node tree and apply stylesheet rules.
    (void)root;
}
